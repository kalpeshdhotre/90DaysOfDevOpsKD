server{
    listen 80;

    location /health {
        default_type text/plain;
        return 200 "healthy\n";
    }

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }
}
